const DEFAULT_PRODUCTS=[{id:'p1',name:'Fresh Marigold Flowers',category:'flowers',price:50,stock:25,image:'',description:'Fresh puja flowers for daily devotion.'},{id:'p2',name:'Daily Puja Kit',category:'kits',price:149,stock:18,image:'',description:'Essential items for your daily puja.'},{id:'p3',name:'Dhoop Sticks',category:'essentials',price:80,stock:30,image:'',description:'Fragrant dhoop for your prayer space.'},{id:'p4',name:'Brass Diya',category:'essentials',price:120,stock:12,image:'',description:'Traditional brass diya for your mandir.'},{id:'p5',name:'Hanuman Ji Puja Kit',category:'kits',price:199,stock:10,image:'',description:'Curated Tuesday Hanuman Ji puja kit.'},{id:'p6',name:'Brass Ganesh Ji Idol',category:'idols',price:699,stock:7,image:'',description:'Elegant brass idol, suitable for home mandir.'}];const read=(k,d)=>JSON.parse(localStorage.getItem(k)||JSON.stringify(d));const write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));let products=read('arpan_products',DEFAULT_PRODUCTS),orders=read('arpan_orders',[]),imageData='';const $=s=>document.querySelector(s);function toast(t){$('#toast').textContent=t;$('#toast').classList.add('show');setTimeout(()=>$('#toast').classList.remove('show'),1600)}function esc(x){return String(x??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function renderProducts(){$('#inventory').innerHTML=products.map(p=>`<div class="inventory-row"><div class="mini-img">${p.image?`<img src="${p.image}">`:'🪔'}</div><div class="inv-main"><b>${esc(p.name)}</b><small>${esc(p.category)} • ${money(p.price)}</small></div><strong>${p.stock}</strong><button class="btn" onclick="editProduct('${p.id}')">Edit</button><button class="btn" onclick="deleteProduct('${p.id}')">Delete</button></div>`).join('')}function money(n){return '₹'+Number(n||0).toLocaleString('en-IN')}function reset(){['pid','pn','pp','ps','pd'].forEach(id=>$('#'+id).value='');imageData='';$('#preview').classList.add('hidden');$('#preview').src='';$('#dropzone').innerHTML='Drag & drop an image here<br><span>or click to choose a file</span><input id="pimg" type="file" accept="image/*" hidden>';bindDrop()}function editProduct(id){const p=products.find(x=>x.id===id);if(!p)return;$('#pid').value=p.id;$('#pn').value=p.name;$('#pc').value=p.category;$('#pp').value=p.price;$('#ps').value=p.stock;$('#pd').value=p.description||'';imageData=p.image||'';if(imageData){$('#preview').src=imageData;$('#preview').classList.remove('hidden')}}function deleteProduct(id){if(!confirm('Delete this product?'))return;products=products.filter(p=>p.id!==id);write('arpan_products',products);renderProducts();toast('Product deleted')}function save(){const name=$('#pn').value.trim();if(!name)return toast('Product name required');const id=$('#pid').value||'p'+Date.now();const old=products.find(p=>p.id===id);const p={id,name,category:$('#pc').value,price:Number($('#pp').value||0),stock:Number($('#ps').value||0),description:$('#pd').value,image:imageData};if(old)products=products.map(x=>x.id===id?p:x);else products.push(p);write('arpan_products',products);renderProducts();reset();toast('Product saved')}function renderOrders(){if(!orders.length){$('#orders').innerHTML='<p class="muted">No orders yet. Orders placed on the storefront appear here.</p>';return}$('#orders').innerHTML=orders.map(o=>`<article class="order-card"><div class="order-head"><div><b>${o.id}</b><small>${new Date(o.date).toLocaleString('en-IN')}</small></div><span class="badge">${esc(o.status)}</span></div><div><b>${esc(o.customer.name)}</b> • ${esc(o.customer.phone)}<br><span class="muted">${esc(o.customer.address)}</span></div><table class="table"><tr><th>Item</th><th>Qty</th><th>Amount</th></tr>${o.items.map(i=>`<tr><td>${esc(i.name)}</td><td>${i.qty}</td><td>${money(i.total)}</td></tr>`).join('')}</table><div class="invoice"><span>Subtotal</span><b>${money(o.subtotal)}</b><span>Delivery</span><b>${money(o.delivery)}</b><strong>Total bill</strong><strong>${money(o.total)}</strong></div><div class="actions"><button class="btn primary" onclick="printInvoice('${o.id}')">Print invoice</button></div></article>`).join('')}function printInvoice(id){const o=orders.find(x=>x.id===id);const w=window.open('','_blank');w.document.write(`<html><head><title>Invoice ${o.id}</title><style>body{font-family:Arial;padding:40px;max-width:700px;margin:auto}table{width:100%;border-collapse:collapse}td,th{padding:8px;border-bottom:1px solid #ddd;text-align:left}.total{font-size:20px;font-weight:bold;text-align:right;margin-top:20px}</style></head><body><h1>Arpan Cart</h1><p>Invoice: ${o.id}<br>${new Date(o.date).toLocaleString('en-IN')}</p><p><b>${esc(o.customer.name)}</b><br>${esc(o.customer.phone)}<br>${esc(o.customer.address)}</p><table><tr><th>Product</th><th>Qty</th><th>Amount</th></tr>${o.items.map(i=>`<tr><td>${esc(i.name)}</td><td>${i.qty}</td><td>${money(i.total)}</td></tr>`).join('')}</table><p>Subtotal: ${money(o.subtotal)}<br>Delivery: ${money(o.delivery)}</p><div class="total">Total: ${money(o.total)}</div><p>Payment: ${esc(o.payment)}</p></body></html>`);w.document.close();w.print()}function bindDrop(){const dz=$('#dropzone'),input=$('#pimg');dz.onclick=()=>input.click();input.onchange=e=>handleFile(e.target.files[0]);['dragover','dragenter'].forEach(ev=>dz.addEventListener(ev,e=>{e.preventDefault();dz.classList.add('drag')}));['dragleave','drop'].forEach(ev=>dz.addEventListener(ev,e=>{e.preventDefault();dz.classList.remove('drag')}));dz.addEventListener('drop',e=>handleFile(e.dataTransfer.files[0]))}function handleFile(file){if(!file||!file.type.startsWith('image/'))return toast('Please select an image');const r=new FileReader();r.onload=()=>{imageData=r.result;$('#preview').src=imageData;$('#preview').classList.remove('hidden')};r.readAsDataURL(file)}document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-tab]').forEach(x=>x.classList.remove('primary'));b.classList.add('primary');$('#productsTab').classList.toggle('hidden',b.dataset.tab!=='products');$('#ordersTab').classList.toggle('hidden',b.dataset.tab!=='orders');if(b.dataset.tab==='orders')renderOrders()});$('#saveProduct').onclick=save;$('#resetProduct').onclick=reset;$('#clearOrders').onclick=()=>{if(confirm('Clear all demo orders?')){orders=[];write('arpan_orders',orders);renderOrders()}};bindDrop();renderProducts();renderOrders();
